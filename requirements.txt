# Install into the project venv (Python 3.12): .venv/bin/pip install -r requirements.txt
chromadb>=1.5,<2
sentence-transformers>=3.0
rank-bm25>=0.2
streamlit>=1.28
pyyaml>=6.0
requests>=2.31
pytest>=8.0
rapidfuzz>=3.0
markdown>=3.5      # Phase 8 Confluence storage-format generation
